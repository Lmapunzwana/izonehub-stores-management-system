package com.izonehub.stores.audit;import org.junit.jupiter.api.Test;import static org.mockito.Mockito.*;class AuditLogServiceTest{@Test void appendsAuditRecord(){AuditLogRepository repo=mock(AuditLogRepository.class);AuditLogService svc=new AuditLogService(repo);svc.record("Item","1","CREATE",null,"{}","admin");verify(repo).save(argThat(a->a.getEntityType().equals("Item")&&a.getAction().equals("CREATE")));}}
